#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: this prints a program using the printf function
 *
 * Return: always 0 on succes
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
